# Cattle Disease Diagnosis Platform - Backend

Python FastAPI backend for cattle disease diagnosis with B2B e-commerce.

## Setup

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```
